[build]
  command   = "npm run build"
  publish   = "dist"

# Redireciona todas as rotas para index.html (necessário para React SPA com PWA)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "24"
